name: Assistant Code Helper
on:
  workflow_dispatch:  # Только ручной запуск

jobs:
  assistant:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}
